- make GeometryFactory->createMULTI*() consistent in taking

  either:
  	const vector<const Geometry *>
	(copy all)
  or:
  	vector<Geometry *>
	(copy none)

  right now they take:
  	const vector<Geometry *>
	(copy vector, get Geometry ownership)
  
- Remove all explicit heap allocations for private static members.
