- Check GeometryFactory *Geometry::factory semantics:
	- owned by Geometry ? 
	- owned by someone else ? 

- Check GeometryEditor behaviour (modify passed geom or create a new one ?)

- Check header files layout

- Check GEOS_VERSION defines and functions

- speedup in BufferSubgraph::computeDepths (read comment there)

- Add class for use in Geometry::userData setting.

- Remove all explicit heap allocations for private static members.
